[install]
# Keep text lockfiles explicit and reviewable.
saveTextLockfile = true

# Save exact versions for future bun add operations.
# This reduces surprise dependency drift; updates should happen through bun update.
exact = true

# Keep the existing supply-chain delay.
minimumReleaseAge = 259200

[install.security]
scanner = "@socketsecurity/bun-security-scanner"
